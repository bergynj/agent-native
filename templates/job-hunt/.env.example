# Server port (production)
PORT=3000

# Custom health check message
PING_MESSAGE=pong

# A2A secret used to sign cross-app calls (e.g. call-agent to mail).
# Optional in dev; the framework falls back to unsigned calls.
# A2A_SECRET=
