---
"@agent-native/core": patch
"@agent-native/dispatch": patch
---

Add opt-in URL sync for durable chat threads and route chat-first templates (chat, assets, and Dispatch) through `/chat/:threadId` deep links.
